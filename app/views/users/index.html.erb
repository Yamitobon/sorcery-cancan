<br><br>
<h1><center><p class="text-success">LISTA DE USUARIOS</p></center></h1>

<br>
<table class="table table-hover table-bordered table-condensed pretty">
<tr>
  <th>Nombre de Usuario</th>
  <th>Email</th>
  <th>Roles</th>
  <th>Acciones</th>
  <th></th>
</tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= user.email %></td>
    <td><%= user.roles_name %></td>
    <td><%= link_to 'Mostrar', user, :class => 'btn btn-mini' %>
	 <%= link_to 'Editar', edit_user_path(user), :class => 'btn btn-mini' %>
	  <%= link_to 'Eliminar', user, :confirm => '¿Esta seguro?', :method => :delete, :class => 'btn btn-mini btn-danger' %>
	  </td>
	  </tr>
	<% end %>
	</table>
	<%= link_to "Crear cuenta", signup_path, :class=>"btn btn-small btn-success" %>
	<br/>

